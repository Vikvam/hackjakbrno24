import type { Meta, StoryObj } from '@storybook/react'

import OptimizeSchedulePage from './OptimizeSchedulePage'

const meta: Meta<typeof OptimizeSchedulePage> = {
  component: OptimizeSchedulePage,
}

export default meta

type Story = StoryObj<typeof OptimizeSchedulePage>

export const Primary: Story = {}
