import type { Meta, StoryObj } from '@storybook/react'

import VysledneSmenyPage from './VysledneSmenyPage'

const meta: Meta<typeof VysledneSmenyPage> = {
  component: VysledneSmenyPage,
}

export default meta

type Story = StoryObj<typeof VysledneSmenyPage>

export const Primary: Story = {}
