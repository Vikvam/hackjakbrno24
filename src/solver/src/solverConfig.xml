<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <!-- Solution and Entity Classes -->
    <solutionClass>ShiftsSchedule</solutionClass>
    <entityClass>ShiftAssignment</entityClass>

    <!-- Score Director Factory -->
    <scoreDirectorFactory>
        <constraintProviderClass>define_constraints</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Termination Configuration -->
    <termination>
        <spentLimit>PT1M30S</spentLimit>
        <unimprovedSpentLimit>PT3M</unimprovedSpentLimit>
        <!-- Commented out best score limit -->
        <!--<bestScoreLimit>0hard/0medium/0soft</bestScoreLimit>-->
    </termination>
</solver>
